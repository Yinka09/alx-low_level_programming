#include <stdio.h>
/**
 * main - A C-program that prints an output using the printf function
 * Return: 0 Always
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,")
	return (0);
}
